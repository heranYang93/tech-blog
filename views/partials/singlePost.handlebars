<section class="card">
  <div>From singlePost</div>
  <a href="/post/:id">
    <header>{{title}}</header>

    <div class="card-body">
      <p>By: {{user.username}}</p>
      <p>By: {{content}}</p>
    </div>
  </a>
</section>